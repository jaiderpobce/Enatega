# 📂 Bitácora de Sesión - 19 de Agosto de 2026

**Proyecto:** Enatega Food Delivery Single-Vendor (Migración Spring Boot + Flutter)  
**Rama Git:** `dev`  
**Último Commit:** `1dfc4c3`  
**Estado:** ✅ Completado y Sincronizado en GitHub  

---

## 🎯 Objetivos de la Sesión de Hoy

1. **Fase 5 (Dashboard & Catálogo Admin):**
   - Creación de categorías y platillos desde el Dashboard de Administración en Flutter y Spring Boot.
2. **Fase 6 (Formulario de Pago por Transferencia, Galería y Chat):**
   - Formulario de comprobante bancario (Banco, Referencia e Imagen de Comprobante).
   - Integración de `image_picker: ^1.2.3` para seleccionar comprobantes reales desde la galería del dispositivo.
   - Creación del Módulo de Chat entre cliente y repartidor.
3. **Fase 7 (Verificación y Aprobación de Pagos en Admin):**
   - Creación de `admin_payments_screen.dart` para revisar transferencias pendientes.
   - Soporte para renderizar comprobantes desde archivos locales (`File`) y URLs en línea (`Image.network`), con modal de amplificación a pantalla completa.
   - Mutación de aprobación/rechazo de pagos (`updatePaymentStatus`) con distintivos de estado en vivo para el cliente.
4. **Fase 8 & 8.1 (Ubicación GPS y Mapa Interactivo Google Maps):**
   - Captura de coordenadas GPS (`latitude`, `longitude`) y dirección de entrega guardadas en MySQL en `placeOrder`.
   - Creación de `LocationPickerScreen`: Pantalla interactiva en mapa completo anclada al GPS del cliente con capacidad de **tocar o arrastrar el marcador** a cualquier otra calle.
   - Renderizado dual (**OpenStreetMap + Google Maps**) para garantizar que el mapa nunca se muestre en blanco.
   - Configuración de API Key de Google Maps `AIzaSyDSMRXq08IjaZ2o5BNh7dBbdTDDZU8XdLw` en Android, iOS y Web.

---

## 📁 Archivos Principales Creados / Modificados Hoy

### ☕ Backend Spring Boot (`backend_springboot/`)
- `src/main/java/com/enatega/model/OrderEntity.java`: Nuevas columnas `bank_name`, `payment_reference`, `payment_proof_url`, `delivery_address`, `latitude`, `longitude`.
- `src/main/resources/graphql/schema.graphqls`: Definición GraphQL actualizada para `Order` y mutaciones `placeOrder` / `updatePaymentStatus`.
- `src/main/java/com/enatega/service/OrderService.java`: Lógica de persistencia en MySQL y mapeo de datos bancarios y coordenadas GPS.
- `src/main/java/com/enatega/controller/OrderGraphQLController.java`: Controlador GraphQL para pagos y ubicaciones.

### 📱 Aplicación Móvil Flutter (`flutter_app/`)
- `lib/features/address/location_picker_screen.dart`: **[NUEVO]** Seleccionador interactivo de ubicación en mapa completo.
- `lib/features/admin/admin_payments_screen.dart`: **[NUEVO]** Pantalla de verificación de comprobantes y aprobación de pagos.
- `lib/features/admin/admin_dashboard_screen.dart`: Panel principal de administración con accesos rápidos.
- `lib/features/cart/cart_screen.dart`: Módulo de ubicación en carrito con botón de mapa antes de pagar.
- `lib/features/cart/payment_proof_screen.dart`: Formulario de comprobante de transferencia con selector de galería.
- `lib/features/orders/order_tracking_screen.dart`: Tarjetas de estado de pago en vivo y coordenadas GPS.
- `lib/core/graphql/graphql_queries.dart`: Consultas GraphQL actualizadas (`myOrdersQuery`, `placeOrderMutation`).
- `android/app/src/main/AndroidManifest.xml`: Configuración de API Key de Google Maps y permisos GPS.
- `ios/Runner/AppDelegate.swift`: Configuración de API Key para iOS.
- `web/index.html`: Script de Google Maps JS para Web.

---

## 🛠️ Instrucciones para Reanudar Mañana

1. **Iniciar Servidores (Docker):**
   ```bash
   cd backend_springboot
   docker-compose up -d
   ```
2. **Iniciar App en Emulador:**
   ```bash
   cd flutter_app
   flutter run -d emulator-5554
   ```

---

*Fecha de registro: 19 de Agosto de 2026*  
*Organizado en `/docs/sesiones/` para seguimiento continuo del proyecto.*
